CREATE DATABASE employee;

USE employee;

CREATE TABLE emp
(
emp_id INT PRIMARY KEY AUTO_INCREMENT,
name VARCHAR(50) NOT NULL,email VARCHAR(100) UNIQUE NOT NULL,
phone VARCHAR(15) NOT NULL,
salary DECIMAL(10,2) NOT NULL DEFAULT 0
);

SELECT * FROM emp;